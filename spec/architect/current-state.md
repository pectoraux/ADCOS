# ADCOS Current State

**Persistent Architect snapshot — W048 accepted-merged; W049 active-authorized under the frozen client-runtime boundary contract at the reconciled post-transition governance baseline.**

## Repository

- Repository: `github.com/pectoraux/ADCOS`
- Baseline: `da56adcef48cd8449c383457949f709f8b5bc9cd` (exact post-transition governance mainline: the DEC-0075/DEC-0076 governance transition was merged by the Architect as PR #140, head `dda16204406c7592d107233771ca74cf3749ba7a`, 2026-09-03T11:46:21Z, carrying the W048 acceptance and the W049 activation; DEC-0077 / LEDGER-RECON-021 are the baseline-advancement-only reconciliation — the DEC-0063/DEC-0066/DEC-0068/DEC-0074 convention — that advances the recorded mainline and the WORK-049-CORE-001 baseline from the issuance baseline `ce1ccaea328743a05cf8d6fa87a114e69d9e253c` to this exact mainline, and the W049 implementation branch is cut from it)
- Architecture version: `1.0`
- Protocol version: `1.0`

## Execution

- Active Work Item: `WORK-049`
- Active authorization: `WORK-049-CORE-001`
- Authorization decision: `DEC-0076`
- Implementation baseline for W049: `da56adcef48cd8449c383457949f709f8b5bc9cd` (the exact reconciled post-transition governance mainline carrying the DEC-0076 activation record; advanced from the issuance baseline `ce1ccaea328743a05cf8d6fa87a114e69d9e253c` by DEC-0077 / LEDGER-RECON-021 after the PR #140 merge; the W049 implementation branch is cut from this baseline, exactly one implementation PR, no self-authorization, no self-merge)
- W048: `accepted-merged` by DEC-0075; PR #139 reviewed at exact correction-round head `e2af4bd20e403c1d4ee9717f7eea8809c16a53cd` (canonical Architect acceptance comment 5524305998), merged `ce1ccaea328743a05cf8d6fa87a114e69d9e253c`; three Architect review rounds over correction heads ba63371 → fc856bb → e2af4bd (P0-1 containment proof semantics, P0-2 quota/containment atomicity, P1-3 recovery gating, then the round-2 content-derived proof digest); 46/46 deterministic sharing/containment battery with byte-identical repeat and PYTHONHASHSEED 0/1/7919/unset determinism; exact-head CI run 33727464067 terminal with the dedicated battery job SUCCESS while the inherited ARCH-02 specification failure stays visible and unmasked (the specification-consistency job is not represented as a green CI result); SOFTWARE-only (the W048 physical containment evidence class remains OPEN and separately governed)
- W047: `accepted-merged` by DEC-0070; PR #135 reviewed at exact correction-round head `348154d063c0e0a12d5635cb2093c67a507a4064`, merged `7bc31f2899307c56639887416d602b41b4c16f43`; 46/46 deterministic marketplace battery; SOFTWARE-only; no successful specification-consistency run on the exact head (inherited ARCH-02/ARCH-06 mainline state, not a W047 regression)
- W046: `accepted-merged` by DEC-0067; PR #132 reviewed at exact correction-round head `09960ea24315e5d0ccfd516d3bdca0802b62d8b7`, merged `f45be6dd0544a2fd6cbc910805def28bbe0c71eb`; 45/45 deterministic battery; SOFTWARE-only
- W045: `accepted-merged` by DEC-0065; PR #129 reviewed at the correction-round head `827234ec3a245e6a9f2f2de5d6525afb495684cc`, merged `a789d9b403d0e2a6e05276bb3cdc2b7d092c6d88`; 46/46 deterministic battery
- W044: `accepted-merged` by DEC-0064; PR #127 reviewed at `6720d220e390999e17707537ab587c1da3b09eb9`, merged `90864ac257a3d93d94852cfa3a74577903f508d3`; 44/44 deterministic battery
- W053: `accepted-merged` by DEC-0062; PR #124 reviewed at `43591667b226b6239e8197816514b679af1e6154`, merged `c9a1f8589cddbbeb21756bdd8f72ed57ea515173`; 44/44 deterministic battery
- W052: `accepted-merged` by DEC-0060
- W051: `accepted-merged` by DEC-0059
- W042: `accepted-merged` by DEC-0057; authorization superseded by DEC-0058
- W041: `accepted-merged` by DEC-0054
- W040: `in-review`, NOT accepted; EVID-007/EVID-008 remain W040-owned and open

## W049

Provider & Buyer Connectivity Client Runtime (issue #98) is now the sole active authorized implementation track. It is a **client/consumer/orchestrator/projection boundary**, not a new authority: the client makes end-user participation possible through a bounded platform-neutral runtime — consent UX, capability discovery, policy presentation, secure handoffs, status/events, and offline/reconnect behavior — while every canonical truth and enforcement stays with its existing owner. It composes the W048 provider sharing runtime (the provider-mode control plane: consent, configuration, status, stop/revoke controls, presentation, handoff), the W047 marketplace discovery/path-selection authority (the only source of presentable offers/candidates), the W046 developer API boundary (behind which it may sit or into which it delegates), W041 NetworkPath machinery (requested/handed off, never validated/activated by the client), W051 CommercialCore lease truth (read-only projection), W042 UsageLedger (surfaced, never duplicated), `/identity` credentials (held/referenced, never minted), `/sessions` (referenced, never created), `/routing` and `/transport` (never reimplemented), `/adapters` platform mechanisms (isolated behind the W049 platform-adapter boundary feeding a platform-neutral core), `/policy` and `/telemetry`, with W045 where applicable and W050 advisory. The frozen client contract — the responsibility model, the provider-mode client lifecycle (UNAVAILABLE → CAPABILITY_CHECKED → READY → CONSENT_REQUIRED → CONSENTED → HANDOFF_REQUESTED → ACTIVE → PAUSED → REVOKED/EXPIRED/STOPPED → CLOSED), the buyer-mode client lifecycle (IDLE → DISCOVERING → OFFER_SELECTED → AUTHORIZATION_PENDING → LEASE_CONFIRMED → PATH_HANDOFF_PENDING → ATTACHING → ACTIVE → DEGRADED/RECONNECTING → EXPIRED/REVOKED/FAILED → CLOSED), explicit/attributable/revocable/fail-closed consent, the ACR-012 capability dimension (unsupported/unknown/supported/restricted) with fail-closed semantics, the platform-adapter boundary, offline/reconnect reconciliation semantics (CANONICAL STATE / LOCAL OBSERVATION / LOCAL INTENT / STALE CACHE / UNKNOWN; never fabricate truth; resume only if canonical authority permits), the emergency-stop control (REQUEST STOP / ENFORCE LOCAL SAFETY → canonical provider-sharing termination → W048 enforcement → traffic termination), the privacy model (minimum-precision location, no raw payment credentials, no unnecessary KYC), the event taxonomy (OBSERVED_CANONICAL_EVENT / LOCAL_UI_EVENT / LOCAL_REQUEST_EVENT / LOCAL_FAILURE), the canonical reason-code policy, the security invariants, and the fail-closed rule (unresolved ambiguity that could produce unauthorized connectivity resolves to DENY/STOP/UNKNOWN) — is recorded in `spec/architect/authorizations/WORK-049.yaml`, `docs/WORK-049-handoff.md`, and `docs/WORK-049-evidence.md`. No new ACR was manufactured for W049: it is not a new architectural authority, adds no new authority vocabulary of its own (the ACR-012 capability vocabulary is reused), and the architecture (declared only in `spec/architecture.md`) is unchanged. Implementation is authorized only from the exact authorized baseline under `WORK-049-CORE-001`, on exactly one implementation PR, with no self-authorization and no self-merge; the implementation handoff is `docs/WORK-049-handoff.md` and the evidence obligations are `docs/WORK-049-evidence.md`. No W049 implementation exists yet.

## Commercial chain

`WORK-051 CommercialCore → WORK-052 UsageLedger → WORK-053 EconomicAllocation → WORK-044 Payment Provider Adapters → WORK-045 Connectivity Eligibility → WORK-046 → WORK-047 → WORK-048 → WORK-049`

W051, W052, W053, W044, W045, W046, W047, and W048 are accepted-merged; W049 is the active implementation track; W050 remains a future candidate.

## Governance

DEC-0075 accepts W048 on PR #139 exact reviewed correction-round head `e2af4bd20e403c1d4ee9717f7eea8809c16a53cd` (canonical Architect acceptance comment 5524305998, merge `ce1ccaea328743a05cf8d6fa87a114e69d9e253c`, 2026-09-03T10:30:47Z) after three Architect review rounds, superseding `WORK-048-CORE-001` and releasing the single active execution slot; the delivery is 20 files, +12565/-0, exactly the authorized literal scope, with the 46/46 deterministic battery, hash-seed determinism, a golden digest stream byte-identical across both correction rounds, and exact-head CI run 33727464067 terminal with the dedicated battery job SUCCESS and the inherited ARCH-02 specification failure separately visible and unmasked. DEC-0076 issues `WORK-049-CORE-001` with the exact issuance baseline `ce1ccaea328743a05cf8d6fa87a114e69d9e253c` (the W048 acceptance merge mainline the governance transition was cut from), the literal scope recorded in `spec/architect/authorizations/WORK-049.yaml` (the `client/` package, the dedicated deterministic client battery, `docs/WORK-049-evidence.md`, `docs/WORK-049-handoff.md`, and additive CI wiring), dependencies exactly WORK-046/WORK-047/WORK-048 (the frozen declaration, all accepted-merged), and the frozen acceptance criteria, transferring the single active execution slot to WORK-049. No new ACR is manufactured: W049 is a client/consumer/projection boundary, not a new authority (the DEC-0071-style authorization gate does not apply; `open_architectural_questions` is empty). The governance transition carrying DEC-0075/DEC-0076 merged as PR #140 (head `dda16204406c7592d107233771ca74cf3749ba7a`, base `ce1ccaea328743a05cf8d6fa87a114e69d9e253c`, merge `da56adcef48cd8449c383457949f709f8b5bc9cd`, 2026-09-03T11:46:21Z, under the single-Architect merge authority of review-protocol §7); DEC-0077 / LEDGER-RECON-021 are the follow-up baseline-advancement-only reconciliation (the DEC-0063/DEC-0066/DEC-0068/DEC-0074 convention) that advances the snapshot baseline and the WORK-049-CORE-001 authorization baseline from `ce1ccaea` to the exact post-transition governance mainline `da56adcef48cd8449c383457949f709f8b5bc9cd` without changing the W049 contract, scope, dependencies, acceptance criteria, authority boundaries, architecture, protocol schemas, the Work Item registry, the dependency DAG, or the W049 authorization identity, and the W049 implementation branch is cut from that exact reconciled mainline (exactly one implementation PR, based on the authorized baseline; no self-authorization; no self-merge; no `spec/architect/` modification from the implementation PR). No frozen architecture semantic, protocol schema, or physical evidence obligation changes; W040 physical obligations remain open and W040-owned; WORK-050 remains an unauthorized ready-candidate. Inherited ARCH-02/ARCH-06 mainline conditions (the execution-ledger RECON-014 trailing-token syntax defect, the DEC-0059 downstream_effect shape, the WORK-051.yaml flow collection, and the open-obligation visibility condition) remain separately disclosed and unchanged.

## Reconciliation

LEDGER-RECON-019 (DEC-0075) records the W048 implementation acceptance: the entry transitions registered → accepted-merged with the exact reviewed/merge facts (reviewed e2af4bd, merge ce1ccae), the WORK-048-CORE-001 supersession, and the snapshot baseline movement bd544db → ce1ccae; after that transition W049 is registered/ready-candidate with authorization none at that instant. LEDGER-RECON-020 (DEC-0076) records the W049 activation (WORK-049-CORE-001, issuance baseline ce1ccae, the exact current main) as the single active authorization with the frozen client-runtime boundary contract (no new ACR, no architecture/registry/DAG change); execution mode returns to implementing. The from/to snapshot SHAs of LEDGER-RECON-020 are equal (ce1ccae) because the W048 acceptance merge mainline is both the reconciled snapshot baseline and the authorization issuance baseline. LEDGER-RECON-021 (DEC-0077) completes the cycle as the baseline-advancement-only reconciliation after PR #140 merged the governance transition at `da56adcef48cd8449c383457949f709f8b5bc9cd` (2026-09-03T11:46:21Z): the snapshot baseline and the WORK-049-CORE-001 authorization baseline advance ce1ccae → da56adc in lockstep, with no W049 contract, scope, dependency, acceptance-criteria, lifecycle, authority-boundary, architecture, protocol-schema, registry, or DAG change, no new authorization issued, and nothing superseded; the W049 implementation branch is cut from the exact reconciled authorized mainline da56adc. No prior work-item history is rewritten; W040 remains independent and unaccepted; WORK-050 remains unauthorized.

LEDGER-RECON-018 (DEC-0074) remains the baseline-advancement-only reconciliation for the DEC-0072/DEC-0073 transition; LEDGER-RECON-016 (DEC-0070) remains the W047 acceptance transition; LEDGER-RECON-017 (DEC-0073) remains the containment-authority freeze and W048 activation; LEDGER-RECON-015 (DEC-0068) remains the baseline-advancement-only reconciliation for the W046 → W047 transition; LEDGER-RECON-014 (DEC-0067) remains the W046 acceptance/W047 activation transition; earlier reconciliations remain authoritative historical records.

## W049 implementation status

No W049 implementation exists at this snapshot: no `client/` package, no dedicated deterministic client battery, no implementation branch, and no delivery. Implementation may begin only from the exact reconciled authorized baseline `da56adcef48cd8449c383457949f709f8b5bc9cd` under `WORK-049-CORE-001` (issued by DEC-0076, baseline advanced by DEC-0077), on exactly one implementation PR, with no self-authorization and no self-merge.
