# ADCOS Repository-Local Implementation Authorizations

Only an authorization record marked `status: active` and `authorized: true` grants implementation permission. A Work Item registration, roadmap status, handoff, issue, PR, CI result, or conversation does not authorize implementation.

Exactly one active implementation authorization is permitted.

The Architect is the sole review, acceptance, and merge authority. Implementation PRs must not modify `spec/architect/`.
